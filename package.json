{
  "name": "seer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "typecheck": "bunx tsc --noEmit",
    "db:backup": "bun src/db-snapshot.ts backup",
    "db:verify": "bun src/db-snapshot.ts verify",
    "db:restore": "bun src/db-snapshot.ts restore"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.1",
    "@types/bun": "latest",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "sharp": "^0.35.3"
  }
}
